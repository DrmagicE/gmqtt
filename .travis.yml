language: go

go:
  - "1.9.x"
  - "1.10.x"
  #- master

install:
  - go get -t -v -d ./...

before_install:
  - go get -t -v -d github.com/DrmagicE/gmqtt/cmd/broker

script:
  -  go test -race ./pkg/packets
  -  go test -race ./server   -coverprofile=coverage.txt -covermode=atomic
  
after_success:
  - bash <(curl -s https://codecov.io/bash)
